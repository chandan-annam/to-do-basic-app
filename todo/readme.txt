need django 3+
suggest to install in virtual env
change settings to ur database (get it by running command py manage.py createproject deleatable) and copy settings there and paste in this file in your pc
later make migrations by running commands py manage.py makemigrations 
                                    and   py manage.py migrate
and run server by using py manage.py runserver 